; rvm get stable
; rvm use ruby --latest --install --default
; gem install bundler
; bundle init
; bundle add rspec
; rspec --init
; mkdir lib
;bundle add pg
;touch lib/database_connection.rb
;touch spec/seeds.sql
;touch app.rb